-- Add a protect dynamic middleware that delete request parameters like date, token and user_id

-- generate from /data/config/auto.js

		list of apiarista commands

-- should /api be renamed to resources ?

-- should '/api' namespace be renamed to '/resources' ?

-- should namespace be an apiarista flag

	apiarista -i --namespace resources